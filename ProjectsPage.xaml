<UserControl x:Class="MyWPFApp.ProjectsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:MyWPFApp">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <WrapPanel x:Name="ProjectsWrapPanel" Orientation="Horizontal"/>
        </ScrollViewer>
        <Button x:Name="btnCreateProject" Grid.Row="1" Content="Create Project" Width="150" Height="30" Margin="10" Background="#FF42A5F5" Foreground="White" Click="btnCreateProject_Click"/>
    </Grid>
</UserControl>